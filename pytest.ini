[pytest]
# Only collect from tests/. scripts/test_models.py is a standalone CLI
# runner whose name collides with pytest's auto-discovery — pinning
# testpaths keeps it out of the default collection.
testpaths = tests
addopts = -ra
